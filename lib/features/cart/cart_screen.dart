import '../../shared/widgets/placeholder_page.dart';

class CartScreen extends PlaceholderPage {
  const CartScreen({super.key})
      : super(
          title: 'Cart',
          subtitle: 'Cart tab scaffolded (auth protected)',
        );
}
