import '../../shared/widgets/placeholder_page.dart';

class AccountScreen extends PlaceholderPage {
  const AccountScreen({super.key})
      : super(
          title: 'Account',
          subtitle: 'Account tab scaffolded',
        );
}
